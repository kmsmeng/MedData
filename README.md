# MedData

To run the program:

1.
<!-- g++ -std=c++17 -o MedData main.cpp HashTable.cpp FileManager.cpp -->
g++ -o MedData main.cpp HashTable.cpp FileManager.cpp
=> This will create a MedData executable fiel

2.
Then run that executable file by running:
./MedData